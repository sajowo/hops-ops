info:
  name: Pobranie listy ofert sprzedazy
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/sales-offers"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
