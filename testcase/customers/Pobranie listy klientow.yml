info:
  name: Pobranie listy klientow
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/customers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
