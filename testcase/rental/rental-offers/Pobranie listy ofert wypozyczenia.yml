info:
  name: Pobranie listy ofert wypozyczenia
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/rental-offers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
