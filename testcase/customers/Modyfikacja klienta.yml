info:
  name: Modyfikacja klienta
  type: http
  seq: 2

http:
  method: PUT
  url: "{{url}}/customers/{{customer_id}}"
  body:
    type: json
    data: |-
      {
        "firstName": "Jan",
        "lastName": "Nowak",
        "address": "Rzeszow, ul. Testowa 2",
        "phoneNumber": "500600701",
        "email": "jan.nowak@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
