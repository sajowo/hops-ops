info:
  name: Rezerwacja sprzetu
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/rental-offers/{{rental_offer_id}}/reservations"
  body:
    type: json
    data: |-
      {
        "equipmentId": 1,
        "customerId": 1,
        "reservedFrom": "2026-06-15",
        "reservedTo": "2026-06-20"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
