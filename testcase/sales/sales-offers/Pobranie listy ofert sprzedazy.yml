info:
  name: Pobranie listy ofert sprzedazy
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/sales-offers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
