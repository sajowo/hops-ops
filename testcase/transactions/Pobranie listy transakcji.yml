info:
  name: Pobranie listy transakcji
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/transactions"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
