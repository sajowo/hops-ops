info:
  name: Aktywacja oferty sprzedazy
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/sales-offers/{{sales_offer_id}}/activate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
