info:
  name: Dezaktywacja oferty sprzedazy
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/sales-offers/{{sales_offer_id}}/deactivate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
