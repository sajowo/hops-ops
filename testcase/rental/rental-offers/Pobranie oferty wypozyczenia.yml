info:
  name: Pobranie oferty wypozyczenia
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/rental-offers/{{rental_offer_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
