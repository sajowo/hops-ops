info:
  name: Pobranie transakcji
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/transactions/{{transaction_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
