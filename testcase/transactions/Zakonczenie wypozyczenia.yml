info:
  name: Zakonczenie wypozyczenia
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/transactions/{{transaction_id}}/finish"
  body:
    type: json
    data: |-
      {
        "finishedAt": "2026-06-20",
        "additionalFee": 0.00
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
