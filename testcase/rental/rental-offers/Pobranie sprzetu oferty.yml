info:
  name: Pobranie sprzetu oferty
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/rental-offers/{{rental_offer_id}}/equipment"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
